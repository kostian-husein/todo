<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
    <style>
        .item {
            width: 300px;
            height: auto;
            background-color: lightgrey;
            border: 1px solid gray;
            border-radius: 6px;
            margin-bottom: 10px;
        }

        .item .label {
            padding: 15px;
            font-weight: 600;
            font-size: 18px;
        }

        .item .description {
            padding: 15px;
        }

        .buttonLink {
            margin-bottom: 10px;
        }

        .buttonLink input {
            width: auto;
            height: 50px;
            font-size: 17px;
            cursor: pointer;
        }
        .createBtn {
            background-color: lightgreen;
        }
        .archiveBtn {
            background-color: lightyellow;
        }

    </style>
    <div style="display: flex; justify-content: center; flex-direction: column; align-items: center">
        <h2>Archive Users list:</h2>
        <a href="/todo/users"> <- Back to users list</a>
        <a href="/todo/deleted-users-list"> Go to deleted users list -></a>


        {% for user in users %}

            <div class="item">
                <a style="float: right; padding: 10px" href="/todo/delete-user/{{ user.userId }}">X</a>
                <a style="float: left; padding: 10px" href="/todo/restore-user/{{ user.userId }}">Restore</a>



                <p class="label">{{ user.login }}</p>
                <p class="description">{{ user.firstName }} {{ user.lastName }}</p>
                <p class="description">{{ user.createdAt.date|date('Y-m-d') }}</p>

            </div>

        {% endfor %}

    </div>

    </body>
</html>
